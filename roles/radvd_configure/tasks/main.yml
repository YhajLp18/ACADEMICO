---
- name: Instalar RADVD
  apt:
    name: radvd
    state: present
    update_cache: yes

- name: Copiar configuración de RADVD
  template:
    src: radvd.conf.j2
    dest: /etc/radvd.conf
  notify:
    - Reiniciar servicio RADVD
