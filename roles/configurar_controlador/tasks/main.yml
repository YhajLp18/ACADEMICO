# ====== 0.1) Defaults / fallbacks seguros (antes de usar controller_user_fallback) ======
- name: "🧭 Definir fallback de usuario si no viene por sudo"
  set_fact:
    controller_user_fallback: "{{ lookup('env','USER') | default('ubuntu', true) }}"
  when: controller_user_fallback is not defined

# Requiere repo_name: asegúrate de pasarlo por vars_files o -e
- name: "🛑 Validar que repo_name esté definido"
  assert:
    that: repo_name is defined
    fail_msg: "Debe definir 'repo_name' (nombre de tu repo) para construir repo_root."

# ====== 0) Detectar usuario y rutas base ======
- name: "🔎 Detectar usuario del controlador"
  set_fact:
    controller_user: "{{ ansible_env.SUDO_USER | default(ansible_user_id) | default(controller_user_fallback) }}"
    repo_root: "/home/{{ ansible_env.SUDO_USER | default(ansible_user_id) | default(controller_user_fallback) }}/{{ repo_name }}"

- name: "📁 Crear /etc/ansible si no existe"
  file:
    path: /etc/ansible
    state: directory
    owner: root
    group: root
    mode: '0755'

# ====== 1) Sistema base ======
- name: "🧱 [1/12] Actualizando repositorios del sistema"
  apt:
    update_cache: yes
  changed_when: false

- name: "🔧 [2/12] Instalando dependencias básicas"
  apt:
    name:
      - python3
      - python3-pip
      - python3-venv
      - git
      - openssh-client
      - curl
      - vim
      - net-tools
      - software-properties-common
    state: present
  tags: deps

- name: "📦 [3/12] Instalando Ansible"
  apt:
    name: ansible
    state: present

# ====== 2) Virtualenv para evitar PEP 668 ======
- name: "📁 Crear entorno virtual en /opt/ansible-venv"
  command: python3 -m venv /opt/ansible-venv
  args:
    creates: /opt/ansible-venv/bin/python

- name: "⬆️ Actualizar pip/setuptools/wheel dentro del venv"
  pip:
    name:
      - pip
      - setuptools
      - wheel
    state: latest
    virtualenv: /opt/ansible-venv
    virtualenv_command: python3 -m venv

- name: "📦 [4/12] Instalar librerías Python necesarias dentro del venv"
  pip:
    name:
      - pyvmomi
      - pywinrm
      - requests
      - jmespath
      - passlib
    state: present
    virtualenv: /opt/ansible-venv
    virtualenv_command: python3 -m venv

# (opcional) Si quieres que el venv pertenezca al usuario no-root
- name: "🔒 Asignar permisos correctos al venv"
  file:
    path: /opt/ansible-venv
    state: directory
    recurse: yes
    owner: "{{ controller_user }}"
    group: "{{ controller_user }}"

# ====== 3) SSH y estructura ======
- name: "📁 Crear carpeta .ssh si no existe"
  file:
    path: "/home/{{ controller_user }}/.ssh"
    state: directory
    mode: '0700'
    owner: "{{ controller_user }}"
    group: "{{ controller_user }}"

- name: "🔑 [5/12] Generar par de claves SSH (si no existe)"
  openssh_keypair:
    path: "/home/{{ controller_user }}/.ssh/id_rsa"
    type: rsa
    size: 2048
    owner: "{{ controller_user }}"
    mode: '0600'
  register: ssh_key

- name: "📂 [6/12] Crear carpeta para proyectos Ansible"
  file:
    path: "/home/{{ controller_user }}/ansible-proyectos"
    state: directory
    mode: '0755'
    owner: "{{ controller_user }}"

- name: "📜 [7/12] Mostrar clave pública generada"
  debug:
    msg: "{{ lookup('file', '/home/' + controller_user + '/.ssh/id_rsa.pub') }}"

# ====== 4) ansible.cfg apuntando a TU repo ======
- name: "⚙️ [8/12] Configurar /etc/ansible/ansible.cfg"
  copy:
    dest: /etc/ansible/ansible.cfg
    content: |
      [defaults]
      inventory = {{ repo_root }}/inventories/hosts.ini
      roles_path = {{ repo_root }}/roles
      host_key_checking = False
      retry_files_enabled = False
      interpreter_python = /opt/ansible-venv/bin/python
      stdout_callback = yaml
    owner: root
    group: root
    mode: '0644'

# ====== 5) Colecciones ======
- name: "📦 [9/12] Instalar colección community.vmware desde Galaxy"
  ansible.builtin.command: ansible-galaxy collection install community.vmware
  register: vmware_install
  changed_when: "'Successfully installed' in vmware_install.stdout or 'already installed' in vmware_install.stdout"

# ====== 3.1) Garantizar carpeta del repo para clonar (después de conocer repo_root) ======
- name: "📂 Crear carpeta destino del repo (si no existe)"
  file:
    path: "{{ repo_root }}"
    state: directory
    owner: "{{ controller_user }}"
    group: "{{ controller_user }}"
    mode: '0755'

# ====== 3.2) (Opcional) Clonar tu repo si pasas repo_url ======
- name: "⬇️ Clonar repo (opcional)"
  git:
    repo: "{{ repo_url }}"
    dest: "{{ repo_root }}"
    version: "{{ repo_branch | default('main') }}"
    accept_hostkey: yes
    update: yes
  when: repo_url is defined
  become_user: "{{ controller_user }}"

# ====== 6) Verificación ======
- name: "📊 [10/12] Verificar instalación de Ansible"
  command: ansible --version
  register: ans_cmd
  changed_when: false

- name: "📊 [11/12] Verificar colecciones de Ansible Galaxy"
  command: ansible-galaxy collection list
  register: galaxy_list
  changed_when: false

- name: "🧩 [12/12] RESULTADO FINAL"
  debug:
    msg: |
      ═══════════════════════════════════════════════════════
      ✅ Instalación completa del controlador Ansible
      ═══════════════════════════════════════════════════════
      👤 Usuario controlador: {{ controller_user }}
      📂 Repo raíz configurado: {{ repo_root }}
      🧠 Versión de Ansible:
      {{ ans_cmd.stdout | default('No disponible') }}
      🧱 Colecciones (vmware):
      {{ (galaxy_list.stdout | default('')) | regex_search('community.vmware[^\n]*', multiline=True) }}
      🗝️ Clave pública SSH:
      /home/{{ controller_user }}/.ssh/id_rsa.pub
      ═══════════════════════════════════════════════════════

# ====== 6.1) Mensaje de próximos pasos (útil en consola) ======
- name: "➡️ Siguiente paso"
  debug:
    msg: |
      ✅ Controlador listo.
      1) Configura IPv6 fija en la NIC LAN (una sola vez):
         sudo tee /etc/netplan/60-lan-ipv6.yaml <<EOF
         network:
           version: 2
           ethernets:
             enp0s8:
               addresses: [ "2025:db8:20::1/64" ]
               dhcp6: no
         EOF
         sudo netplan apply

      2) Si no clonaste automático:
         git clone <TU_REPO> {{ repo_root }}

      3) Ejecuta:
         cd {{ repo_root }}
         ansible-playbook -i inventories/hosts.ini site.yml
